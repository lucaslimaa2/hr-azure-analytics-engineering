# Copy this file to .streamlit/secrets.toml (gitignored) for local dev,
# or paste these keys into the app's Settings -> Secrets on Streamlit Cloud.
# Use the read-only bi_reader login, never the SQL admin.

SQL_SERVER   = "hr-synapse-780hz7-ondemand.sql.azuresynapse.net"
SQL_DATABASE = "hr_curated"
SQL_USER     = "bi_reader"
SQL_PASSWORD = "the-bi_reader-password"
